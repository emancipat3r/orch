---
- name: VPS Setup with WireGuard
  hosts: all
  become: yes
  vars:
    wireguard_port: 51820
    wireguard_interface: wg0

  pre_tasks:
    - name: Gather OS facts
      setup:
        gather_subset:
          - "!all"
          - "!any"
          - distribution
          - distribution_version
          - distribution_major_version

    - name: Check if OS is Ubuntu or Debian
      fail:
        msg: "This playbook only supports Ubuntu and Debian distributions. Detected: {{ ansible_distribution }}"
      when: ansible_distribution not in ['Ubuntu', 'Debian']

    - name: Display OS information
      debug:
        msg: "Running on {{ ansible_distribution }} {{ ansible_distribution_version }}"

  tasks:
    - name: Stop and disable unattended-upgrades
      systemd:
        name: unattended-upgrades
        state: stopped
        enabled: no
      ignore_errors: yes

    - name: Kill any running apt/dpkg processes
      shell: |
        pkill -9 apt-get || true
        pkill -9 dpkg || true
        pkill -9 unattended-upgrade || true
        sleep 2
      ignore_errors: yes

    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Wait for apt locks to be released
      shell: |
        while fuser /var/lib/dpkg/lock >/dev/null 2>&1 || \
            fuser /var/lib/apt/lists/lock >/dev/null 2>&1 || \
            fuser /var/cache/apt/archives/lock >/dev/null 2>&1; do
          echo "Waiting for other apt operations to complete..."
          sleep 5
        done
      args:
        executable: /bin/bash
      changed_when: false

    - name: Install essential packages
      apt:
        name:
          - curl
          - wget
          - git
          - netcat-openbsd
          - socat
          - ufw
          - qrencode
          - net-tools
          - iptables-persistent
        state: present
      retries: 3
      delay: 10
      register: result
      until: result is not failed

    - name: Install WireGuard
      apt:
        name:
          - wireguard
          - wireguard-tools
        state: present

    - name: Enable IP forwarding
      sysctl:
        name: net.ipv4.ip_forward
        value: "1"
        state: present
        reload: yes

    - name: Enable IPv6 forwarding
      sysctl:
        name: net.ipv6.conf.all.forwarding
        value: "1"
        state: present
        reload: yes

    - name: Generate WireGuard server private key
      shell: wg genkey
      register: wg_server_private_key
      changed_when: false
      no_log: true

    - name: Generate WireGuard server public key
      shell: echo "{{ wg_server_private_key.stdout }}" | wg pubkey
      register: wg_server_public_key
      changed_when: false

    - name: Generate WireGuard client private key
      shell: wg genkey
      register: wg_client_private_key
      changed_when: false
      no_log: true

    - name: Generate WireGuard client public key
      shell: echo "{{ wg_client_private_key.stdout }}" | wg pubkey
      register: wg_client_public_key
      changed_when: false

    - name: Create WireGuard configuration directory
      file:
        path: /etc/wireguard
        state: directory
        mode: "0700"

    - name: Create WireGuard server configuration
      template:
        src: ../templates/server_wg_config
        dest: /etc/wireguard/wg0.conf
        mode: "0600"
      notify: restart wireguard

    - name: Create client remote configuration directory
      file:
        path: /root/wireguard-client
        state: directory
        mode: "0700"

    - name: Create client configuration file
      ansible.builtin.template:
        src: ../templates/client_wg_config
        dest: /root/wireguard-client/client.conf
        mode: "0600"

    - name: Refresh package facts
      ansible.builtin.package_facts:
        manager: auto

    - name: Disable UFW if installed
      ansible.builtin.ufw:
        state: disabled
      when: "'ufw' in ansible_facts.packages"

    - name: Configure SSHD
      lineinfile:
        path: /etc/ssh/sshd_config
        regexp: "{{ item.regexp }}"
        line: "{{ item.line }}"
        validate: "/usr/sbin/sshd -t -f %s"
      loop:
        - {
            regexp: "^.PasswordAuthentication.*",
            line: "PasswordAuthentication no",
          }
        - { regexp: "^.AllowTcpForwarding.*", line: "AllowTcpForwarding yes" }
        - { regexp: "^.GatewayPorts.*", line: "GatewayPorts yes" }
        - {
            regexp: "^.PubkeyAuthentication.*",
            line: "PubkeyAuthentication yes",
          }
        - {
            regexp: "^.PermitRootLogin.*",
            line: "PermitRootLogin prohibit-password",
          }
      notify: restart ssh

    - name: Start and enable WireGuard
      systemd:
        name: wg-quick@wg0
        state: started
        enabled: yes

    - name: Display server public key
      debug:
        msg: "WireGuard Server Public Key: {{ wg_server_public_key.stdout }}"

    - name: Create Ansible facts directory
      file:
        path: /etc/ansible/facts.d
        state: directory
        mode: "0755"

    - name: Save WireGuard keys to local facts
      copy:
        content: |
          {
            "server_private_key": "{{ wg_server_private_key.stdout }}",
            "server_public_key": "{{ wg_server_public_key.stdout }}",
            "client_private_key": "{{ wg_client_private_key.stdout }}",
            "client_public_key": "{{ wg_client_public_key.stdout }}"
          }
        dest: /etc/ansible/facts.d/wireguard.fact
        mode: "0600"

  handlers:
    - name: restart wireguard
      systemd:
        name: wg-quick@wg0
        state: restarted

    - name: restart ssh
      systemd:
        name: ssh
        state: restarted
